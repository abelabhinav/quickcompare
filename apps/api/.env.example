DATABASE_URL="postgresql://postgres:postgres@localhost:5432/quickcompare"

# Future legitimate provider integrations. Mock Blinkit/Zepto adapters do not
# require these values.
PROVIDER_INTEGRATIONS_ENABLED=false
PROVIDER_DEFAULT_TIMEOUT_MS=3000
PROVIDER_DEFAULT_RETRIES=1
PROVIDER_DEFAULT_RETRY_DELAY_MS=150
